# Frontend

React + TypeScript + Vite client for the Test Case Generator. See the [repository root README](../README.md) for full setup instructions and architecture notes.

Quick start:

```bash
cp .env.example .env   # points at the backend, defaults to http://localhost:3001
npm install
npm run dev
```

Runs on `http://localhost:5173` by default.
